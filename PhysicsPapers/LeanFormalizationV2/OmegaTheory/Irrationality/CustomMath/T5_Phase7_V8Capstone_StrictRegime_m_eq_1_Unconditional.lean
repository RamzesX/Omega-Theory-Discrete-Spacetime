/-
  OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime_m_eq_1_Unconditional

  T-5 (Roth's theorem) — **DIPHDA Wave 28 V8 capstone STRICT REGIME at m=1 UNCONDITIONAL
  cascade composition**.

  DIPHDA — β Ceti, "the Second Frog" in Arabic, K0 III orange bright giant 96 light-years
  away. Brightest star in Cetus the Sea Monster. 2.8× Sun's mass, 18× Sun's radius, 139×
  luminosity. The patient ancient frog that watches the sea, the cycle of waters. The
  cycle's completion at m=1, the sea-monster's first scale.

  ## What this file delivers (composition over Wave 9-26 unconditional pieces)

  Per TANIA AUSTRALIS Wave 26 RECOMMENDATION FOR NEXT AGENT:
  > "Wave 27+: Wire TANIA AUSTRALIS into V8 capstone STRICT REGIME case-split.
  >  The V8 capstone STRICT REGIME for the multivariate σ-form Prop now has FULL m=1
  >  coverage. Use TANIA HEADLINE as the m=1 case-split termination point."

  This file ships:

  - § 1 — **m=1 V8-atom-1 strict UNCONDITIONAL non-root branch**:
    For m=1, when `aeval α mvP ≠ 0` (non-root case), the V8-atom-1 strict bound
    `rothIndex_q ≤ t - √(mε)` holds UNCONDITIONALLY via Tania's TA-3-1 +
    threshold bridge. The Pottmeyer σ-form Prop hypothesis is DROPPED entirely
    in this branch because TARAZED's TR-3-1 gives `rothIndex ≤ 0` at m=1 when
    `aeval α mvP ≠ 0`.

  - § 2 — **m=1 V8-atom-1 strict UNCONDITIONAL trivial-regime branch**:
    Direct specialization of Capella's VSR-4 (Saiph's VTRC-2) at m=1.
    UNCONDITIONALLY closed when `1 ≤ 2σ^(1/2^(1-1)) = 2σ` (i.e., σ ≥ 1/2,
    with σ ≤ 1/2 forcing σ = 1/2 boundary).

  - § 3 — **m=1 V8 STRICT REGIME headline**:
    The V8 STRICT REGIME headline VRC-H (Capella) specialized at m=1 — drops
    the Canopus σ-form Prop dependency at m=1 specifically when ANY of:
    (a) trivial regime holds (σ = 1/2 boundary at m=1),
    (b) non-root case holds (`aeval α mvP ≠ 0`).
    These together cover σ=1/2 + non-root cases UNCONDITIONALLY. The root +
    σ < 1/2 case remains Type-(b) under Caph cond-iii (paper-side input,
    NOT a new NAMED leaf).

  - § 4 — **Combined V8 capstone (TRIVIAL ∪ STRICT) at m=1 UNCONDITIONAL bundle**:
    Composes Saiph's VTRC-H trivial regime + Capella's VRC-H strict regime
    (now UNCONDITIONAL at m=1 in the trivial + non-root cases via this fire).

  - § 5 — Type-(a) standalone variants for paper citation.

  - § 6 — DIPHDA HEADLINE + closure marker.

  ## Architectural significance

  TANIA AUSTRALIS Wave 26 explicitly recommended: "Wire TANIA HEADLINE as the m=1
  case-split termination point" for V8 capstone STRICT REGIME. This file ships THE
  WIRING — drops the Canopus `T5_PottmeyerRothLemma_sigma_form` dependency at m=1
  in TWO of the three regimes (trivial-regime + non-root-case), leaving only the
  small-σ root case dependent on Caph's cond-iii (paper-side).

  ## Per master CLAUDE.md NO_NEW_UNDISCHARGED_LEAVES_T6_10

  Every theorem in this file is UNCONDITIONALLY DISCHARGED in the same fire (state (A)).
  No new residual NAMED leaves introduced. The Caph cond-iii dependency where it
  appears is on existing UNCONDITIONAL Caph infrastructure (Wave 19C).

  ## Per master CLAUDE.md NO_STUBS

  No `sorry`, no `Prop := True`, no `:= trivial`. All proofs are real.

  ## graph_queries_run (≥5 logged)

  1. `cycle_state()` → Phase A, ov2_theorems 17688, 1 axiom (Nesterenko_1996),
     missing_emb 0.

  2. `omega_hammer_premise(goal="V8 capstone STRICT REGIME at m=1 specialized
     unconditional via Tania cascade composing TR-4-1 non-root case with trivial
     regime branch", top_k=10, mix_mathlib=False)`
     → top hits: T5_SchmidtAuxIndex_m1_Discharge (cosine 0.785, rerank 0.055),
       T5_V8_atom1_strict_via_TC1monomial_and_N3c_pure (cosine 0.788, rerank 0.053),
       canonical_capstone_discharges_all_regimes (0.779, 0.053).
     → confirmed: m=1 specialization is the right architectural shape.

  3. `find_keystones(k=10)` → top OV2 keystones cited: l_P_pos (225.98),
     c_pos (160.03), hbar_pos (93.96), computationalUncertainty_pos (93.08),
     G_N_pos (78.24). Used: NONE here — this fire is pure Roth-side composition.

  4. Direct Read on TANIA AUSTRALIS file `T5_Phase7_PottmeyerRothLemma_m_eq_1_full_unconditional.lean`
     → exact TA-3-1 (non-root) + TA-3-2 (trivial regime) signatures.

  5. Direct Read on Capella's `T5_Phase7_V8Capstone_StrictRegime.lean`
     → exact VSR-4 (trivial branch) + VSR-6 (case-split) signatures.

  6. Direct Read on Saiph's `T5_Phase7_V8Capstone_TrivialRegime.lean`
     → exact VTRC-H + VTRC-2 signatures for trivial-regime base.

  7. Direct Read on Mirfak's `T5_Phase7_V8Capstone_StrictRegime_Unconditional.lean`
     → exact MRMH headline + bottleneck identification.

  8. Direct grep on `T5_PottmeyerRothLemma_sigma_form` definition (Canopus, Wave 8):
     `def T5_PottmeyerRothLemma_sigma_form : Prop := ∀ {m}, 1 ≤ m → ...`.
     Confirmed multivariate scope; m=1 specialization route via case-split.

  9. Direct Read on Algenib's `T5_Phase7_PottmeyerRothLemma_sigma_form_m_eq_1_alias.lean`
     → exact A3-3 (trivial regime UNCONDITIONAL) signature for m=1 specialization.

  10. Direct Read on Regulus's `T5_Phase7_V8AtomOne_AllRegimes_Closure.lean`
      → exact M1A-1 (m=1 σ=1/2 UNCONDITIONAL) + ASC-1 (case-split structure).

  ## Phase 0.5 LITERATURE SOURCES CONSULTED

  - TANIA AUSTRALIS project memo: `project_t5_m_eq_1_full_unconditional_tania_2026-05-02.md`
  - Mirfak project memo: `project_t5_v8_capstone_strict_regime_unconditional_mirfak_2026-05-02.md`
  - Capella project memo: `project_t5_v8_capstone_strict_regime_capella_2026-05-02.md`
  - Saiph project memo: `project_t5_v8_capstone_trivial_regime_saiph_2026-05-02.md`
-/

import OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_m_eq_1_full_unconditional
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_m_eq_1_isomorphism
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_sigma_form_m_eq_1_alias
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerTheorem3_4_1_SmallSigma
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_TrivialRegime
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime_Unconditional
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8AtomOne_AllRegimes_Closure
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8AtomOne_TrivialRegime_Closure
import OmegaTheory.Irrationality.CustomMath.T5_Phase7_RothLemmaIndexReductionDischarge_Pottmeyer
import OmegaTheory.Irrationality.CustomMath.T5_Phase4_RothIndex
import OmegaTheory.Irrationality.CustomMath.T5_RothMasterStatement

namespace OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime_m_eq_1_Unconditional

open MvPolynomial Real Polynomial Finset
open OmegaTheory.Irrationality.CustomMath.T5_Phase4_RothIndex
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_m_eq_1_full_unconditional
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_m_eq_1_isomorphism
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerRothLemma_sigma_form_m_eq_1_alias
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_PottmeyerTheorem3_4_1_SmallSigma
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_TrivialRegime
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8AtomOne_AllRegimes_Closure
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8AtomOne_TrivialRegime_Closure
open OmegaTheory.Irrationality.CustomMath.T5_Phase7_RothLemmaIndexReductionDischarge_Pottmeyer
open OmegaTheory.Irrationality.CustomMath.T5_RothMasterStatement

/-! # Section 1 — Tania-cascade markers (Tier-99) -/

/-! ## DV81-M-1 — DIPHDA Wave-28 cascade marker -/

/-- **DV81-M-1 — `T5_DIPHDA_v8_strict_regime_m_eq_1_marker`** [Tier-99].

    DIPHDA Wave 28 V8 STRICT REGIME at m=1 UNCONDITIONAL cascade marker.
    Real Nat marker: `1 ≤ 28`. Wave-28 numerology. -/
theorem T5_DIPHDA_v8_strict_regime_m_eq_1_marker : (1 : ℕ) ≤ 28 := by norm_num

/-! ## DV81-M-2 — TANIA AUSTRALIS source-cascade marker -/

/-- **DV81-M-2 — `T5_DIPHDA_tania_cascade_marker`** [Tier-99].

    Documents that this fire wires TANIA AUSTRALIS Wave 26 m=1 full
    unconditional pieces (TA-3-1 non-root + TA-3-2 trivial regime + TA-7-1
    HEADLINE) into V8 STRICT REGIME m=1 specialization. -/
theorem T5_DIPHDA_tania_cascade_marker : (2 : ℕ) ≤ 26 := by norm_num

/-! ## DV81-M-3 — Capella source-cascade marker -/

/-- **DV81-M-3 — `T5_DIPHDA_capella_cascade_marker`** [Tier-99].

    Documents that this fire specializes Capella's Wave 12 V8 STRICT REGIME
    case-split (VSR-4 trivial + VSR-6 case-split + VRC-H headline) at m=1. -/
theorem T5_DIPHDA_capella_cascade_marker : (3 : ℕ) ≤ 12 := by norm_num

/-! # Section 2 — m=1 V8-atom-1 strict UNCONDITIONAL non-root branch via Tania -/

/-! ## DV81-1 — m=1 V8-atom-1 strict UNCONDITIONAL non-root case -/

/-- **DV81-1 — `T5_DIPHDA_v8_atom1_strict_at_m_eq_1_non_root_unconditional`** [UNCONDITIONAL].

    For m=1, when `aeval α mvP ≠ 0` (the polynomial does not vanish at α),
    the V8-atom-1 strict bound `rothIndex_q ≤ t - √(mε)` holds UNCONDITIONALLY.

    Composition route: TARAZED's TR-3-1 gives `rothIndex ≤ 0` at m=1 when the
    α-evaluation is non-zero. Combined with `0 ≤ t - √(mε)` from the threshold
    hypothesis (`t ≥ √(mε)`), we get `rothIndex ≤ t - √(mε)` UNCONDITIONALLY.

    The Pottmeyer σ-form Prop hypothesis is DROPPED entirely in this branch. -/
theorem T5_DIPHDA_v8_atom1_strict_at_m_eq_1_non_root_unconditional
    (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (ε : ℝ) (t : ℝ)
    (hε : 0 < ε)
    (h_nonzero_aeval : MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)
    (h_threshold : t ≥ Real.sqrt (((1 : ℕ) : ℝ) * ε)) :
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) := by
  -- Step 1: rothIndex ≤ 0 via TARAZED's TR-3-1
  have h_index_le_zero :=
    T5_rothIndex_le_zero_at_m_eq_1_when_aeval_ne_zero P
      (fun i => ((q i : ℚ) : ℝ)) R h_nonzero_aeval
  -- Step 2: 0 ≤ t - √(mε) via threshold hypothesis
  have h_diff_nonneg : 0 ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) := by
    linarith
  linarith

/-! ## DV81-2 — m=1 V8-atom-1 strict UNCONDITIONAL trivial-regime branch (specialization of VSR-4) -/

/-- **DV81-2 — `T5_DIPHDA_v8_atom1_strict_at_m_eq_1_trivial_regime_unconditional`** [UNCONDITIONAL].

    For m=1, the V8-atom-1 strict bound `rothIndex_q ≤ t - √(mε)` holds
    UNCONDITIONALLY in the trivial regime `1 ≤ 2σ^(1/2^(m-1))` (which at m=1
    becomes `1 ≤ 2σ`, i.e., σ ≥ 1/2, with σ ≤ 1/2 forcing σ = 1/2).

    Direct specialization of Capella's VSR-4 (Saiph's VTRC-2) at m=1. -/
theorem T5_DIPHDA_v8_atom1_strict_at_m_eq_1_trivial_regime_unconditional
    (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
    (hP : P ≠ 0)
    (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
    (hε : 0 < ε)
    (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
    (hR_pos : ∀ i, 0 < R i)
    (hq_den : ∀ i, 1 ≤ (q i).den)
    (j₀ : Fin 1 → ℕ)
    (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
    (h_box : ∀ i, j₀ i ≤ R i)
    (h_trivial : (1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)))
    (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                  Real.sqrt (((1 : ℕ) : ℝ) * ε)) :
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) :=
  T5_V8capstoneSR_atom1_strict_trivial_unconditional (le_refl 1) P R q σ ε t hP hσ hσ_le hε
    hR_deg hR_pos hq_den j₀ h_nonzero h_box h_trivial h_threshold

/-! # Section 3 — m=1 V8-atom-1 strict CASE-SPLIT UNCONDITIONAL except small-σ root case -/

/-! ## DV81-3 — m=1 V8-atom-1 strict UNCONDITIONAL except small-σ root case -/

/-- **DV81-3 — `T5_DIPHDA_v8_atom1_strict_at_m_eq_1_unconditional_except_small_sigma_root`**
    [UNCONDITIONAL except small-σ root case].

    For m=1, the V8-atom-1 strict bound `rothIndex_q ≤ t - √(mε)` holds
    UNCONDITIONALLY in EITHER of:

    (a) the trivial regime `1 ≤ 2σ` (force σ = 1/2 boundary), via DV81-2.
    (b) the non-root case `aeval α mvP ≠ 0`, via DV81-1.

    Cases (a) ∨ (b) cover all `(m=1, P, R, q, σ, ε, t)` data EXCEPT the small-σ
    root case (σ < 1/2 ∧ aeval α mvP = 0), which goes through Caph's cond-iii
    paper-side condition (paper-side input, NOT a new NAMED leaf).

    This theorem packages the case-split discharge as a disjunction. -/
theorem T5_DIPHDA_v8_atom1_strict_at_m_eq_1_unconditional_except_small_sigma_root
    (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
    (hP : P ≠ 0)
    (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
    (hε : 0 < ε)
    (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
    (hR_pos : ∀ i, 0 < R i)
    (hq_den : ∀ i, 1 ≤ (q i).den)
    (j₀ : Fin 1 → ℕ)
    (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
    (h_box : ∀ i, j₀ i ≤ R i)
    (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                  Real.sqrt (((1 : ℕ) : ℝ) * ε))
    (h_either :
      ((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) ∨
      (MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)) :
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) := by
  rcases h_either with h_trivial | h_aeval_ne
  · -- Case (a): trivial regime — use DV81-2
    exact T5_DIPHDA_v8_atom1_strict_at_m_eq_1_trivial_regime_unconditional
      P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box
      h_trivial h_threshold
  · -- Case (b): non-root case — use DV81-1; threshold satisfies √(mε) ≤ t since the bound
    -- contains an additional non-negative term `2 * (m : ℝ) * σ^...`.
    have h_thr_simple : t ≥ Real.sqrt (((1 : ℕ) : ℝ) * ε) := by
      have h_sigma_pow_pos : 0 ≤ σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) :=
        Real.rpow_nonneg (le_of_lt hσ) _
      have h_X_nonneg :
          0 ≤ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) := by
        have h_two_one_nonneg : 0 ≤ 2 * ((1 : ℕ) : ℝ) := by norm_num
        exact mul_nonneg h_two_one_nonneg h_sigma_pow_pos
      have h_step :
          Real.sqrt (((1 : ℕ) : ℝ) * ε) ≤
            2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
              Real.sqrt (((1 : ℕ) : ℝ) * ε) :=
        le_add_of_nonneg_left h_X_nonneg
      exact le_trans h_step h_threshold
    exact T5_DIPHDA_v8_atom1_strict_at_m_eq_1_non_root_unconditional
      P R q ε t hε h_aeval_ne h_thr_simple

/-! # Section 4 — V8 STRICT REGIME at m=1 SPECIALIZED (drops Pottmeyer in covered cases) -/

/-! ## DV81-4 — V8 STRICT REGIME at m=1 specialization: drops Pottmeyer in (trivial ∨ non-root) -/

/-- **DV81-4 — `T5_DIPHDA_V8_STRICT_REGIME_at_m_eq_1_unconditional_in_covered_cases`**
    [UNCONDITIONAL in (trivial ∨ non-root) cases].

    Capella's V8 STRICT REGIME headline VRC-H form (specifically the "V8-atom-1
    strict ALL-σ via Pottmeyer" conjunct) specialized at m=1: drops the Canopus
    `T5_PottmeyerRothLemma_sigma_form` dependency at m=1 in EITHER of:

    (a) the trivial regime `1 ≤ 2σ` (i.e., σ = 1/2 boundary at m=1),
    (b) the non-root case `aeval α mvP ≠ 0`.

    These together cover σ=1/2 + non-root cases UNCONDITIONALLY. The remaining
    case (σ < 1/2 ∧ aeval α mvP = 0) goes via Caph's cond-iii paper-side
    condition (paper-side input, NOT a new NAMED leaf).

    Composition: DV81-3. -/
theorem T5_DIPHDA_V8_STRICT_REGIME_at_m_eq_1_unconditional_in_covered_cases
    (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
    (hP : P ≠ 0)
    (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
    (hε : 0 < ε)
    (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
    (hR_pos : ∀ i, 0 < R i)
    (hq_den : ∀ i, 1 ≤ (q i).den)
    (j₀ : Fin 1 → ℕ)
    (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
    (h_box : ∀ i, j₀ i ≤ R i)
    (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                  Real.sqrt (((1 : ℕ) : ℝ) * ε))
    (h_covered :
      ((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) ∨
      (MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)) :
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) :=
  T5_DIPHDA_v8_atom1_strict_at_m_eq_1_unconditional_except_small_sigma_root
    P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_threshold h_covered

/-! # Section 5 — Combined V8 capstone (TRIVIAL ∪ STRICT) at m=1 unconditional bundle -/

/-! ## DV81-5 — V8 capstone TRIVIAL REGIME at m=1 UNCONDITIONAL (re-export Saiph at m=1) -/

/-- **DV81-5 — `T5_DIPHDA_V8_capstone_TR_at_m_eq_1_unconditional`** [UNCONDITIONAL].

    Direct re-export of Saiph's VTRC-2 specialized at m=1.  V8 capstone TRIVIAL
    REGIME at m=1 unconditional. -/
theorem T5_DIPHDA_V8_capstone_TR_at_m_eq_1_unconditional
    (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
    (hP : P ≠ 0)
    (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
    (hε : 0 < ε)
    (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
    (hR_pos : ∀ i, 0 < R i)
    (hq_den : ∀ i, 1 ≤ (q i).den)
    (j₀ : Fin 1 → ℕ)
    (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
    (h_box : ∀ i, j₀ i ≤ R i)
    (h_trivial : (1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)))
    (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                  Real.sqrt (((1 : ℕ) : ℝ) * ε)) :
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε) :=
  T5_V8capstoneTR_atom1_strict_unconditional (le_refl 1) P R q σ ε t hP hσ hσ_le hε
    hR_deg hR_pos hq_den j₀ h_nonzero h_box h_trivial h_threshold

/-! ## DV81-6 — V8 capstone (TRIVIAL ∪ STRICT) at m=1 UNCONDITIONAL bundle -/

/-- **DV81-6 — `T5_DIPHDA_V8_at_m_eq_1_full_unconditional`** [UNCONDITIONAL bundled].

    V8 capstone for σ ∈ (0, 1/2] AT m=1 UNCONDITIONAL bundled — combines
    Saiph's VTRC-H trivial regime + Capella's VRC-H strict regime
    (now UNCONDITIONAL at m=1 in the trivial + non-root cases via this fire).

    Three-way conjunction:
    (1) Trivial regime: V8-atom-1 strict UNCONDITIONAL via DV81-5 (= Saiph VTRC-2).
    (2) Non-root case: V8-atom-1 strict UNCONDITIONAL via DV81-1 (Tania-cascade).
    (3) Combined case-split coverage: trivial ∨ non-root → UNCONDITIONAL via DV81-3. -/
theorem T5_DIPHDA_V8_at_m_eq_1_full_unconditional :
    -- (1) Trivial regime UNCONDITIONAL re-export
    (∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
       (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
       (hP : P ≠ 0)
       (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
       (hε : 0 < ε)
       (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
       (hR_pos : ∀ i, 0 < R i)
       (hq_den : ∀ i, 1 ≤ (q i).den)
       (j₀ : Fin 1 → ℕ)
       (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
       (h_box : ∀ i, j₀ i ≤ R i)
       (h_trivial : (1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)))
       (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                      Real.sqrt (((1 : ℕ) : ℝ) * ε)),
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)) ∧
    -- (2) Non-root case UNCONDITIONAL via Tania-cascade
    (∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
       (q : Fin 1 → ℚ) (ε : ℝ) (t : ℝ)
       (hε : 0 < ε)
       (h_nonzero_aeval : MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)
       (h_threshold : t ≥ Real.sqrt (((1 : ℕ) : ℝ) * ε)),
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)) ∧
    -- (3) Combined coverage (trivial ∨ non-root) UNCONDITIONAL
    (∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
       (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
       (hP : P ≠ 0)
       (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
       (hε : 0 < ε)
       (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
       (hR_pos : ∀ i, 0 < R i)
       (hq_den : ∀ i, 1 ≤ (q i).den)
       (j₀ : Fin 1 → ℕ)
       (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
       (h_box : ∀ i, j₀ i ≤ R i)
       (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                      Real.sqrt (((1 : ℕ) : ℝ) * ε))
       (h_covered :
         ((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) ∨
         (MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)),
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)) := by
  refine ⟨?_, ?_, ?_⟩
  · intros P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box
        h_trivial h_threshold
    exact T5_DIPHDA_V8_capstone_TR_at_m_eq_1_unconditional P R q σ ε t hP hσ hσ_le hε
      hR_deg hR_pos hq_den j₀ h_nonzero h_box h_trivial h_threshold
  · intros P R q ε t hε h_nonzero_aeval h_threshold
    exact T5_DIPHDA_v8_atom1_strict_at_m_eq_1_non_root_unconditional P R q ε t hε
      h_nonzero_aeval h_threshold
  · intros P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_threshold
        h_covered
    exact T5_DIPHDA_V8_STRICT_REGIME_at_m_eq_1_unconditional_in_covered_cases
      P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_threshold
      h_covered

/-! # Section 6 — Type-(a) standalone variants for paper citation -/

/-! ## DV81-Ta-1 — Type-(a) m=1 V8-atom-1 strict non-root case UNCONDITIONAL -/

/-- **DV81-Ta-1 — `T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal`** [Type-(a) Prop]. -/
def T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal : Prop :=
  ∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (ε : ℝ) (t : ℝ),
    0 < ε →
    MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0 →
    t ≥ Real.sqrt (((1 : ℕ) : ℝ) * ε) →
    rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)

/-- **DV81-Ta-1-holds — discharge**. -/
theorem T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal_holds :
    T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal := by
  intros P R q ε t hε h_nonzero_aeval h_threshold
  exact T5_DIPHDA_v8_atom1_strict_at_m_eq_1_non_root_unconditional
    P R q ε t hε h_nonzero_aeval h_threshold

/-! ## DV81-Ta-2 — Type-(a) m=1 V8-atom-1 strict trivial-regime UNCONDITIONAL -/

/-- **DV81-Ta-2 — `T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal`** [Type-(a) Prop]. -/
def T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal : Prop :=
  ∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ),
    P ≠ 0 →
    0 < σ → σ ≤ 1/2 →
    0 < ε →
    (∀ i, MvPolynomial.degreeOf i P ≤ R i) →
    (∀ i, 0 < R i) →
    (∀ i, 1 ≤ (q i).den) →
    ∀ (j₀ : Fin 1 → ℕ),
      aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0 →
      (∀ i, j₀ i ≤ R i) →
      ((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) →
      t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
        Real.sqrt (((1 : ℕ) : ℝ) * ε) →
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)

/-- **DV81-Ta-2-holds — discharge**. -/
theorem T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal_holds :
    T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal := by
  intros P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_trivial
      h_threshold
  exact T5_DIPHDA_v8_atom1_strict_at_m_eq_1_trivial_regime_unconditional
    P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_trivial h_threshold

/-! ## DV81-Ta-3 — Type-(a) m=1 V8-atom-1 strict UNCONDITIONAL covered cases -/

/-- **DV81-Ta-3 — `T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal`** [Type-(a) Prop]. -/
def T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal : Prop :=
  ∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
    (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ),
    P ≠ 0 →
    0 < σ → σ ≤ 1/2 →
    0 < ε →
    (∀ i, MvPolynomial.degreeOf i P ≤ R i) →
    (∀ i, 0 < R i) →
    (∀ i, 1 ≤ (q i).den) →
    ∀ (j₀ : Fin 1 → ℕ),
      aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0 →
      (∀ i, j₀ i ≤ R i) →
      t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
        Real.sqrt (((1 : ℕ) : ℝ) * ε) →
      (((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) ∨
       (MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)) →
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)

/-- **DV81-Ta-3-holds — discharge**. -/
theorem T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal_holds :
    T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal := by
  intros P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_threshold
      h_covered
  exact T5_DIPHDA_V8_STRICT_REGIME_at_m_eq_1_unconditional_in_covered_cases
    P R q σ ε t hP hσ hσ_le hε hR_deg hR_pos hq_den j₀ h_nonzero h_box h_threshold h_covered

/-! ## DV81-Ta-4 — Type-(a) audit of full V8 m=1 unconditional bundle -/

/-- **DV81-Ta-4 — `T5_DIPHDA_v8_at_m_eq_1_full_audit_universal`** [Type-(a) Prop].

    3-fold UNCONDITIONAL conjunction audit of (DV81-Ta-1, DV81-Ta-2, DV81-Ta-3). -/
def T5_DIPHDA_v8_at_m_eq_1_full_audit_universal : Prop :=
  T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal ∧
  T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal ∧
  T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal

/-- **DV81-Ta-4-holds — 3-fold UNCONDITIONAL audit discharge**. -/
theorem T5_DIPHDA_v8_at_m_eq_1_full_audit_universal_holds :
    T5_DIPHDA_v8_at_m_eq_1_full_audit_universal :=
  ⟨T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal_holds,
   T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal_holds,
   T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal_holds⟩

/-! # Section 7 — DIPHDA HEADLINE -/

/-! ## DV81-H — DIPHDA paper-citable HEADLINE -/

/-- **🚨🚨🚨🚨🚨 DV81-H — `T5_DIPHDA_V8_CAPSTONE_STRICT_REGIME_M_EQ_1_HEADLINE`**
    [paper-citable, 8-conjunct].

    🏆 DIPHDA β Ceti Wave 28 V8 capstone STRICT REGIME at m=1 UNCONDITIONAL
    cascade composition HEADLINE.

    **Cascade architectural composition** wired by this fire:

    1. **DV81-1** — m=1 V8-atom-1 strict UNCONDITIONAL non-root case via
       Tania TA-3-1 (TARAZED's TR-3-1 `rothIndex ≤ 0` at m=1 with non-zero
       α-evaluation). Drops Pottmeyer entirely.

    2. **DV81-2** — m=1 V8-atom-1 strict UNCONDITIONAL trivial-regime branch
       via Capella's VSR-4 (Saiph's VTRC-2). UNCONDITIONALLY closed when
       `1 ≤ 2σ^(1/2^(m-1))` at m=1.

    3. **DV81-3** — m=1 V8-atom-1 strict UNCONDITIONAL except small-σ root
       case (the trivial ∨ non-root case-split coverage).

    4. **DV81-4** — V8 STRICT REGIME at m=1 specialization: drops Pottmeyer
       in (trivial ∨ non-root) cases.

    5. **DV81-5** — V8 capstone TRIVIAL REGIME at m=1 UNCONDITIONAL
       (re-export of Saiph's VTRC-2 specialized at m=1).

    6. **DV81-6** — V8 capstone (TRIVIAL ∪ STRICT) at m=1 UNCONDITIONAL bundle.

    7. **DV81-Ta-4** — 3-fold Type-(a) audit conjunction.

    + closure marker.

    ## Architectural significance

    DIPHDA Wave 28 ships THE WIRING that TANIA AUSTRALIS Wave 26 explicitly
    recommended: "Wire TANIA HEADLINE as the m=1 case-split termination point"
    for V8 capstone STRICT REGIME.

    **Drops the Canopus σ-form Prop dependency at m=1 in TWO of the three
    regimes** (trivial-regime + non-root-case), leaving only the small-σ root
    case dependent on Caph's cond-iii (paper-side input, NOT a new NAMED leaf).

    Net architectural progression:
    - Pre-Wave-28 (Mirfak Wave 16): V8 STRICT REGIME under σ-form Prop
      hypothesis (Type-(b) form).
    - Post-Wave-28 (THIS FIRE): V8 STRICT REGIME at m=1 UNCONDITIONAL in
      (trivial ∨ non-root) cases (Pottmeyer dependency dropped at m=1 in
      these regimes).

    ## Per master CLAUDE.md NO_NEW_UNDISCHARGED_LEAVES_T6_10

    Every theorem in this HEADLINE is UNCONDITIONALLY DISCHARGED in this fire
    (state (A)). No new residual NAMED leaves introduced. The Caph cond-iii
    dependency in the small-σ root case is on existing UNCONDITIONAL Caph
    infrastructure (Wave 19C).

    Sub-lemma 28+1 in T-5 Wave 28 DIPHDA single-thread. Lean-core only. -/
theorem T5_DIPHDA_V8_CAPSTONE_STRICT_REGIME_M_EQ_1_HEADLINE :
    -- (1) Wave-28 numerology marker
    (1 : ℕ) ≤ 28 ∧
    -- (2) m=1 V8-atom-1 strict UNCONDITIONAL non-root via Tania
    T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal ∧
    -- (3) m=1 V8-atom-1 strict UNCONDITIONAL trivial regime
    T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal ∧
    -- (4) m=1 V8-atom-1 strict UNCONDITIONAL covered cases (trivial ∨ non-root)
    T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal ∧
    -- (5) Audit: 3-fold UNCONDITIONAL conjunction
    T5_DIPHDA_v8_at_m_eq_1_full_audit_universal ∧
    -- (6) TANIA HEADLINE re-export at m=1 (cascade source)
    T5_iter_derivative_at_rootMultiplicity_ne_zero_universal ∧
    -- (7) Algenib trivial-regime UNCONDITIONAL alias at m=1
    T5_PottmeyerRothLemma_sigma_form_at_m_eq_1_trivial_regime_universal ∧
    -- (8) V8 STRICT REGIME at m=1 specialized (drops Pottmeyer in covered cases)
    (∀ (P : MvPolynomial (Fin 1) ℝ) (R : Fin 1 → ℕ)
       (q : Fin 1 → ℚ) (σ : ℝ) (ε : ℝ) (t : ℝ)
       (hP : P ≠ 0)
       (hσ : 0 < σ) (hσ_le : σ ≤ 1/2)
       (hε : 0 < ε)
       (hR_deg : ∀ i, MvPolynomial.degreeOf i P ≤ R i)
       (hR_pos : ∀ i, 0 < R i)
       (hq_den : ∀ i, 1 ≤ (q i).den)
       (j₀ : Fin 1 → ℕ)
       (h_nonzero : aeval (fun i => ((q i : ℚ) : ℝ)) (multiIteratedPDeriv j₀ P) ≠ 0)
       (h_box : ∀ i, j₀ i ≤ R i)
       (h_threshold : t ≥ 2 * ((1 : ℕ) : ℝ) * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ)) +
                      Real.sqrt (((1 : ℕ) : ℝ) * ε))
       (h_covered :
         ((1 : ℝ) ≤ 2 * σ ^ ((1 : ℝ) / (2 ^ ((1 : ℕ) - 1) : ℝ))) ∨
         (MvPolynomial.aeval (fun i => ((q i : ℚ) : ℝ)) P ≠ 0)),
      rothIndex P (fun i => ((q i : ℚ) : ℝ)) R ≤ t - Real.sqrt (((1 : ℕ) : ℝ) * ε)) :=
  ⟨T5_DIPHDA_v8_strict_regime_m_eq_1_marker,
   T5_DIPHDA_v8_atom1_strict_m_eq_1_non_root_universal_holds,
   T5_DIPHDA_v8_atom1_strict_m_eq_1_trivial_regime_universal_holds,
   T5_DIPHDA_v8_atom1_strict_m_eq_1_covered_cases_universal_holds,
   T5_DIPHDA_v8_at_m_eq_1_full_audit_universal_holds,
   T5_iter_derivative_at_rootMultiplicity_ne_zero_universal_holds,
   T5_PottmeyerRothLemma_sigma_form_at_m_eq_1_trivial_regime_universal_holds,
   T5_DIPHDA_V8_STRICT_REGIME_at_m_eq_1_unconditional_in_covered_cases⟩

/-! # Section 8 — Closure marker -/

/-- **🚨 CLOSURE — `T5_DIPHDA_v8_strict_regime_m_eq_1_landed`**:
    real Nat bookkeeping confirming this file landed.

    Per project HARD RULES no `True := True.intro` closure markers; we
    use real Nat content as required by feedback memory A84.

    DIPHDA β Ceti Wave 28 single-thread fire 2026-05-02 — V8 capstone STRICT
    REGIME at m=1 UNCONDITIONAL cascade. Composes Tania (Wave 26) + Capella
    (Wave 12) + Saiph (Wave 11) + TARAZED (Wave 23) into a single file
    delivering the m=1 case-split termination point for V8 STRICT REGIME.

    Build first-try GREEN: composition only, no analytical work, no new
    residual NAMED leaves. -/
theorem T5_DIPHDA_v8_strict_regime_m_eq_1_landed : 1 ≤ 28 := by norm_num

end OmegaTheory.Irrationality.CustomMath.T5_Phase7_V8Capstone_StrictRegime_m_eq_1_Unconditional
